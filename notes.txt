create environment by : python -m venv env
 pep8 styling guidelines for managing code structure
 NameError is caused when we try to access a variable that is not defined.
 a=4
 print(name) # NameError

 
# wrong type causes TypeError
name = hari
value = 8
#print(name + value)  # cannot concatenate string and int

Comparision operator can't be used in strings
 OR= Any one true all condition true
 And= Any one condition false all condition false
 Not= Opposite of the given condition 
#IndexError 
Index out of range 
#KeyError
 
Single line IF
Both condition are checked in one line itself

Nested IF
If can be in middle of another if elif blocks for multiple number of times.

----------List----------------


#To add data we have 3 ways 
# 1.append-adds data at last, takes only one value
# 2. insert- adds at any index 
# 3. extends- mixes 2 list doesn't create new list. Make changes in either on of the list  
# 4.concat-mixes 2 list, doesn't change data creates new list.Old list os as it is.

md-markdown


Technology
Ollama-Can create chatgpt
Ollama+Python+Streamlit
huggingface
Ollama+paidmodels
Playright

rebase-the process of moving or combining a sequence of commits to a new base commit
CI/CD-continuous integration/ deployment
pydantic validation-check for dynamic 

Clone-
json
file not found error
ioUnsupportedOperation 




Pull request:
1. Make a new pull request
2. Create pull request
3. Give proper name and description and send it 
4. Make sure when you are pushing the branch it should be the one you are working on and not the main branch


-----Dictionary-----
1.Curly bracket
2.Works in key value pair
3. Key can't be dupe value can be
4. dict is keyword
5.dict inside dict is possible and list inside dict is also possible


------Loop------
1. for - loop contains start and end value
2. while - works until the condition is true 

---Range--
1. (1,100,1)- last 1 is increment
2. (100,1,-1)
3.(100,10,1)- range not created but no error occurs 

---Break & Continue-----
--------
1. Various cases present such as lower upper 

a=["hi","hey","bye"]
print(random.choice(a))


-------Tuples-------
1.Immutable
2.Similar to list but uses this()
3.Methods arent used
4.Data are kept in Index and accessed accordingly
5.Dupe allowed

---Set---
1.Immutable
2.No method can be used
3. Each time different order
4.Doesnt support dupe
5. Written in {}
6. Can be used in Loop 

---Functions---
1.return add, a, b,c and return [add,a,b,c] is different thing
Local variable, global variable
2. to make local variable global 
a=1
def testing():
  a=[1,2,3,4]
  global datas

  datas=100
print(datas)


Default value
1. Only last value can be default value

Arbitary Positional 
1.*args ,*kwargs
2. Multiple data at the same time

Hierarchy 
Positional argument - arbitary args - kwargs

MRO- Multiple class auda kun lai priority dini
Data Access Modifier - Encapsulation 

__a=1==private
_a=1==protected

